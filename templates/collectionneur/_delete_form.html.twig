<form method="post" action="{{ path('app_collectionneur_delete', {'id': p06_collectionneur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ p06_collectionneur.id) }}">
    <button class="btn">Delete</button>
</form>
