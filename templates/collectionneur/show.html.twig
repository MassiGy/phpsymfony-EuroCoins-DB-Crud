{% extends 'base.html.twig' %}

{% block title %}P06Collectionneur{% endblock %}

{% block body %}
    <h1 class="mb-3">P06Collectionneur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ p06_collectionneur.id }}</td>
            </tr>
            <tr>
                <th>CollectionneurNom</th>
                <td>{{ p06_collectionneur.CollectionneurNom }}</td>
            </tr>
            <tr>
                <th>CollectionneurPrenom</th>
                <td>{{ p06_collectionneur.CollectionneurPrenom }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex">
        <a class="btn btn-primary mr-3" href="{{ path('app_collectionneur_index') }}">back to list</a>

        <a class="btn btn-warning mr-3" href="{{ path('app_collectionneur_edit', {'id': p06_collectionneur.id}) }}">edit</a>

        {{ include('collectionneur/_delete_form.html.twig') }}

    </div>
{% endblock %}
