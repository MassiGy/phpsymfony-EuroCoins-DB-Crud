{% extends 'base.html.twig' %}

{% block title %}P06PieceTranche{% endblock %}

{% block body %}
    <h1 class="mb-5">P06PieceTranche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ p06_piece_tranche.id }}</td>
            </tr>
            <tr>
                <th>PieceTranche</th>
                <td>{{ p06_piece_tranche.PieceTranche }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex">
        <a class="btn btn-primary mr-3"  href="{{ path('app_piece_tranche_index') }}">back to list</a>

        <a class="btn btn-warning mr-3" href="{{ path('app_piece_tranche_edit', {'id': p06_piece_tranche.id}) }}">edit</a>

        {{ include('piece_tranche/_delete_form.html.twig') }}
    </div>
{% endblock %}
