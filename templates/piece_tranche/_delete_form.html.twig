<form method="post" action="{{ path('app_piece_tranche_delete', {'id': p06_piece_tranche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ p06_piece_tranche.id) }}">
    <button class="btn">Delete</button>
</form>
